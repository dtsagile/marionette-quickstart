(function () {
    'use strict';
    dts.HelloModel = Backbone.Model.extend({

        defaults: {
            name: 'No Name',
        },

        initialize: function (options) {
            //code
        }

    });

})();